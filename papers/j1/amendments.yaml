# Paper 1 protocol amendments — pre-specified changes only.
# Append here BEFORE outcome analysis when scope must change.

protocol_version: "1.0"
locked_at: "2026-07-01"
git_commit_at_lock: null  # set when supervisor signs off
status: pilot  # pilot | frozen | submitted

amendments:
  - id: amd-002
    date: 2026-07-01
    reason: >
      Baseline targets yaml had AIME/GPQA-scale bands incorrectly applied to MATH-500,
      causing false-pass at ~60% and false-fail at ~93%.
    change: >
      Audit all tasks in qrm_literature_targets.yaml against QRM Table 1; add
      self-documenting provenance to compare_qrm_baseline.py. b01 archives scored
      with old yaml are invalid for gate comparison.
    made_before_outcome_analysis: true
    approved_by: team
  - id: amd-003
    date: 2026-07-01
    reason: >
      QRM source attribution corrected after reading appendix tables: Table 1 is Qwen-only;
      Llama-8B baselines are Appendix B Table 4. GPQA tolerance widened to ±8pp (n=198).
      GPQA rows tagged gate=sanity (sober profile); only reproduction MATH-500/GSM8K are hard gates.
    change: >
      Update qrm_literature_targets.yaml refs/sources; per-row tolerance_pp and gate type;
      compare_qrm_baseline exits 1 on hard_passed only.
    made_before_outcome_analysis: true
    approved_by: team
# Example entry (do not delete — copy format when needed):
#  - id: amd-001
#    date: 2026-07-18
#    reason: GPQA Hugging Face access delayed
#    change: Run MATH-500 + GSM8K pilot before GPQA block b07
#    made_before_outcome_analysis: true
#    approved_by: supervisor
