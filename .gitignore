# Python
__pycache__/
.pytest_cache/
*.pyc
.venv/
.env
.qrm_official_env_ready
*.egg-info/

# Local dump files (never commit)
FULL_CODE_BACKUP.md
FULL_PROJECT_BACKUP.md

# LaTeX build intermediates
*.aux
*.bbl
*.blg
*.log
*.out
*.synctex.gz
*.fdb_latexmk
*.fls
*.xdv

# Experiments
runs/
# Structured evaluation summaries, validation records, and analysis reports are tracked
!results/
logs/
outputs-win5080-*/
outputs-hpc-*/

# Literature PDFs over GitHub 100 MB limit (text .md extract is tracked instead)
docs/literature/paper1/ALL_PAPERS_MERGED.pdf

# Large files (repo root only)
/models/
/quant/
*.safetensors
*.bin
*.pt
*.gguf

# Cloned upstream repos (setup via scripts/hpc/qrm_parity/setup_official_qrm_repo.sh)
external/

# Cache
.cache/
hf_cache/
.wandb/
wandb/

# Secrets
.env
.env.local
.github-token
*.pem

# Stale matplotlib exports (submitted PDF uses TikZ in paper/main.tex)
paper_figures/*.png
paper_figures/*.pdf
